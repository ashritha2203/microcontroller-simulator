Flags are basically small signals used by the CPU to know what happened after an operation.

For example, if the answer of an operation is 0, the Zero Flag is set.

 If an addition produces a carry, the Carry Flag is set. Similarly, the Sign Flag can show whether the result is negative.

There are also other flags that help the CPU identify conditions like overflow.
